---
type Props = {
  as?: string;
  class?: string;
};

const Tag = Astro.props.as ?? "div";
const className = Astro.props.class ?? "";
---

<Tag class:list={["mx-auto w-full max-w-6xl px-6", className]}>
  <slot />
</Tag>

